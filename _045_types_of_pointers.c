#include <stdio.h>
#include <stdlib.h>

int main(){
    int x = 69;
    float y = 4.20;

    // Void pointer (General purpose pointer)
    // void* ptr;
    // ptr = &x;
    // printf("%d", *((int*)ptr));

    // NULL ((void*)0) pointer
    // int* ptr = NULL; // Do not dreference here

    // Dangling pointer (points to freed memory location or location whose content has been deleted)
    
    // Wild pointer (Uninitialized pointer)

    return 0;
}