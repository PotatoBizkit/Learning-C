#include <stdio.h>

int main(){
    char s[300];
    FILE* ptr = NULL;

    // Reading a file
    // ptr = fopen("abc.txt", "r");
    // fscanf(ptr, "%s", s); // reads only til first white space
    // printf("%s", s);
    // fclose(ptr);
    return 0;
}